---
name: claude-seo
description: Enforce 100% indexing optimization and 0.0 CLS following the strict architectural guidelines inside CLAUDE.md and the impeccable docs.
---

# claude-seo

You are operating under the `claude-seo` skill. When invoked for SEO optimizations, you must rigorously adhere to the following:

1. **Architectural Guidelines**: Always reference and comply with the rules defined in `CLAUDE.md` at the project root.
2. **Impeccable Design Context**: Ensure all changes align perfectly with the slow-and-smooth luxury tech aesthetic defined in `.impeccable.md` (and the impeccable docs workspace directory).
3. **Core Web Vitals & SEO**: Maintain a strict 0.0 CLS layout stability constraint. Ensure dynamic array integrity, canonical URL matching, and dynamic caching configurations so search engines like Google can always parse the latest XML payloads.
4. **Plugin Resources**: Utilize any scripts or utilities found in the `claude-seo` plugin folder at the project root as necessary.

When auditing or modifying Next.js SEO and routing architecture, ALWAYS double-check:
- Dynamic data maps are safe and won't throw runtime undefined errors.
- Metadata wrappers use absolute canonicals without implicit trailing slash mismatches.
- `robots.ts` and `sitemap.ts` prevent stale cache locks using `force-dynamic`.
